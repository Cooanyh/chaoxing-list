
## 2026-01-10 进展

- **代码重构**:
    - 全面清理了 `chaoxing-list.user.js` 中的冗余注释和 AI 生成风格的解释，提升了代码整洁度。
    - 归档了之前实验性的 iframe 数据提取功能，保留了基础的 DOM 遍历与文本标签查找逻辑（`extractDataFromDomByLabel`）以备后用。
- **文档更新**:
    - 更新 `README.md` 至 v2.0.0，包含 UI 重构、仪表盘、待办任务聚合等主要功能特性。
- **版本发布**:
    - 版本号更新为 **2.0.0**，标志着 UI 重构版本的正式发布。
    - 完成代码提交与推送。

## 2026-03-12 进展（v2.1.0）

- **布局修复**:
    - 课程进度卡片（`.progress-card`）在宽屏下左右两列等宽问题已修复。
    - 给 `.progress-item` 添加 `min-width: 0` 和 `overflow: hidden`，防止内容撑破 grid 列，消除横向滚动条。
    - 移动端（`max-width: 768px`）下，进度卡片从跨双列改为单列堆叠（`grid-column: span 1`）。

- **忽略功能（新增）**:
    - 所有板块（待办任务、作业、考试、课程任务、课程进度）的每条课程信息均添加了 🚫 忽略按钮（hover 时显示）。
    - 点击忽略按钮弹出二次确认对话框，列出将被忽略的项目名称，支持点击遮罩取消。
    - 支持多选忽略：在"查看全部"详情页顶部点击"多选"切换多选模式，每项显示复选框，底部出现"忽略所选(N)"批量操作栏。
    - 忽略状态通过 `localStorage` 持久化存储（键名 `chaoxing_ignored_items`），刷新后保持。
    - 各板块使用 Vue `computed` 过滤已忽略项，忽略后立即从列表中消失。
    - 在"查看全部"详情页顶部新增"🚫 已忽略(N)"按钮，点击进入已忽略内容面板：
        - 显示所有已忽略项（标题、课程、忽略日期）。
        - 每项有"撤销忽略"按钮，支持多选后批量恢复。

- **版本号**:
    - 版本号更新为 **2.1.0**。
    - 完成代码提交与推送。

## 2026-03-13 进展（v2.2.0）

- **忽略功能优化**:

## 2026-01-10 进展

- **代码重构**:
    - 全面清理了 `chaoxing-list.user.js` 中的冗余注释和 AI 生成风格的解释，提升了代码整洁度。
    - 归档了之前实验性的 iframe 数据提取功能，保留了基础的 DOM 遍历与文本标签查找逻辑（`extractDataFromDomByLabel`）以备后用。
- **文档更新**:
    - 更新 `README.md` 至 v2.0.0，包含 UI 重构、仪表盘、待办任务聚合等主要功能特性。
- **版本发布**:
    - 版本号更新为 **2.0.0**，标志着 UI 重构版本的正式发布。
    - 完成代码提交与推送。

## 2026-03-12 进展（v2.1.0）

- **布局修复**:
    - 课程进度卡片（`.progress-card`）在宽屏下左右两列等宽问题已修复。
    - 给 `.progress-item` 添加 `min-width: 0` 和 `overflow: hidden`，防止内容撑破 grid 列，消除横向滚动条。
    - 移动端（`max-width: 768px`）下，进度卡片从跨双列改为单列堆叠（`grid-column: span 1`）。

- **忽略功能（新增）**:
    - 所有板块（待办任务、作业、考试、课程任务、课程进度）的每条课程信息均添加了 🚫 忽略按钮（hover 时显示）。
    - 点击忽略按钮弹出二次确认对话框，列出将被忽略的项目名称，支持点击遮罩取消。
    - 支持多选忽略：在"查看全部"详情页顶部点击"多选"切换多选模式，每项显示复选框，底部出现"忽略所选(N)"批量操作栏。
    - 忽略状态通过 `localStorage` 持久化存储（键名 `chaoxing_ignored_items`），刷新后保持。
    - 各板块使用 Vue `computed` 过滤已忽略项，忽略后立即从列表中消失。
    - 在"查看全部"详情页顶部新增"🚫 已忽略(N)"按钮，点击进入已忽略内容面板：
        - 显示所有已忽略项（标题、课程、忽略日期）。
        - 每项有"撤销忽略"按钮，支持多选后批量恢复。

- **版本号**:
    - 版本号更新为 **2.1.0**。
    - 完成代码提交与推送。

## 2026-03-13 进展（v2.2.0）

- **忽略功能优化**:
    - 实现了忽略内容的板块隔离，即某个板块（如"待办任务"）忽略的项目仅显示并在该板块的"已忽略"面板中撤销。
    - 工具函数（`getItemKey`、`ignoreItems` 等）现已支持 `sectionType`（板块类型）进行差异化存储。
    - 在课程进度看板（`.progress-card`）标题区域独立添加了"🚫 已忽略"入口。
    - 每个课程进度的项目新增了在 Hover 时可见的忽略按钮。
    - 在课程看板内部实现了专门针对课程进度的局部已忽略面板展示、恢复及多选批量恢复。
    
- **版本发布**:
    - 更新版本号至 **2.2.0**。
    - 完成代码提交推送至 GitHub。

## 2026-03-13 进展（v2.3.0）

- **忽略按钮 UI 优化**:
    - 调整忽略按钮（🚫）为**常显状态**（由 Hover 显示改为默认半透明常显，Hover 全显），提升交互直观性。
    - 将所有板块的忽略按钮移动到列表/卡片边缘，避免干扰正文展示。
- **课程进度布局重构**:
    - 重构了课程进度卡片的内部布局。
    - 建立了右侧信息列，将“完成率”、“任务点统计”与“忽略按钮”进行**垂直对齐排列**，解决了按钮突兀且未对齐的问题。
- **版本更新**:
    - 脚本版本号升级至 **2.3.0**。
    - 完成相关代码提交与推送。

## 2026-03-13 进展（v2.3.1）

- **Bug 修复**:
    - 修复了在仪表盘首页卡片（待办、作业、考试、课程任务）中点击忽略按钮后确认无效的问题。
    - 修复原因是未正确传递板块参数（`sectionType`）。
- **版本发布**:
    - 脚本版本号升级至 **2.3.1**。
    - 完成相关代码提交与推送。

## 2026-03-13 进展（v2.3.2）

- **重大 Bug 修复**:
    - 修复了仪表盘完全不显示的重大问题。
    - 原因是 `@match` 元数据匹配规则在之前的编辑中被误删，导致脚本在 `i.chaoxing.com` 等关键域名下不加载。
    - 现已恢复完整的元数据声明。
- **版本发布**:
    - 脚本版本号升级至 **2.3.2**。
    - 已同步代码推送。
