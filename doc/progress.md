## 2026-04-03 进展（v2.2.1）

- **课程进度查询防检测功能（新增）**:
    - 问题背景：原有的 `Promise.all` 并发请求所有课程进度，速度太快容易触发系统验证码
    - 解决方案：添加延迟机制 + 查询开关
    - 新增存储键：
        - `chaoxing_progress_enabled`：课程进度查询开关状态
        - `chaoxing_progress_delay`：请求延迟时间（默认500ms）
    - 新增管理函数：
        - `isProgressEnabled()`：检查查询功能是否启用
        - `setProgressEnabled(enabled)`：设置查询功能开关
        - `getProgressDelay()`：获取当前延迟时间
        - `setProgressDelay(delay)`：设置延迟时间
        - `delay(ms)`：异步延迟工具函数
    - 修改 `loadAllCourseProgress()` 函数：
        - 检查开关状态，关闭时跳过查询
        - 使用 for 循环替代 `Promise.all` 并发请求
        - 每个请求间隔默认等待500ms
        - 添加详细进度日志（正在获取第X/Y个课程）
    - UI 界面更新：
        - 在课程进度卡片头部添加查询开关按钮
        - 显示当前延迟时间设置
        - 开关状态显示："🔍 查询中"（开启）或 "❌ 查询已关闭"（关闭）
        - 添加禁用样式（灰色背景，cursor: not-allowed）
    - localStorage 持久化：
        - 开关状态和延迟时间都会保存到 localStorage
        - 刷新页面后设置保持不变

- **版本号更新**:
    - chaoxing-list.user.js: 2.2.0 → 2.2.1
    - README.md: 版本徽章更新为 2.2.1
    - 更新描述信息：添加 v2.2.1 功能说明

- **代码提交**:
    - 完成本功能的所有代码修改
    - 更新 README.md 更新日志
    - 更新进展文档

## 2026-04-02 进展（v2.2.0 补充）

- **课程进度全屏查看（新增）**:
    - 为课程进度板块添加"查看全部"按钮，点击后在页面容器内全屏查看
    - 采用响应式网格布局（`display: grid`），每行自动填充卡片
    - 保持原有卡片样式和悬浮窗功能，等比放大方便查看
    - 卡片增加 hover 效果：`z-index: 99999` 确保悬停卡片在顶部
    - 悬浮窗 `z-index` 提升至 100000，避免被其他卡片遮挡

- **AI实践和分组任务 API 集成（新增）**:
    - 集成专用 API 获取 AI 实践数据：`https://stat2-ans.chaoxing.com/stat2/study-data/ai-evaluate-stat`
    - 集成专用 API 获取分组任务数据：`https://stat2-ans.chaoxing.com/stat2/study-data/groupTask`
    - 新增 `checkAllDone()` 函数，统一管理多个 API 调用的完成计数
    - 保留 HTML 数据提取作为备用方案，确保数据完整性
    - 成功计数从 2 提升至 5，包含：job API、point API、AI实践 API、分组任务 API、学习任务页面

- **智能排序优化**:
    - 新增 `hasData()` 函数判断课程是否有有效数据（积分、排名、测验、AI实践、分组任务）
    - 有数据的课程优先显示，查询不到数据的课程自动后排
    - 在有数据的课程中，按任务完成进度排序（完成度少的在前）
    - 排序逻辑应用于加载数据和查看全部页面两种场景

- **悬浮窗优化与 Bug 修复**:
    - 修复查看全部页面悬浮窗不显示的问题
    - 添加 `.detail-progress-item:hover .progress-tooltip { display: block }` 选择器
    - 添加 `.detail-progress-item .progress-tooltip` 响应式样式（居中显示在下方）
    - 清理悬浮窗中的调试 `console.log` 语句
    - 清理文件末尾冗余空行

- **代码清理**:
    - 移除所有悬浮窗调试代码
    - 移除冗余空行
    - 更新 README.md 更新日志，记录本次所有更新内容

- **版本号**:
    - 版本号保持 **2.2.0**

## 2026-04-02 进展（v2.2.0 补充）

- **课程进度全屏查看（新增）**:
    - 为课程进度板块添加"查看全部"按钮，点击后在页面容器内全屏查看
    - 采用响应式网格布局（`display: grid`），每行自动填充卡片
    - 保持原有卡片样式和悬浮窗功能，等比放大方便查看
    - 卡片增加 hover 效果：`z-index: 99999` 确保悬停卡片在顶部
    - 悬浮窗 `z-index` 提升至 100000，避免被其他卡片遮挡

- **AI实践和分组任务 API 集成（新增）**:
    - 集成专用 API 获取 AI 实践数据：`https://stat2-ans.chaoxing.com/stat2/study-data/ai-evaluate-stat`
    - 集成专用 API 获取分组任务数据：`https://stat2-ans.chaoxing.com/stat2/study-data/groupTask`
    - 新增 `checkAllDone()` 函数，统一管理多个 API 调用的完成计数
    - 保留 HTML 数据提取作为备用方案，确保数据完整性
    - 成功计数从 2 提升至 5，包含：job API、point API、AI实践 API、分组任务 API、学习任务页面

- **智能排序优化**:
    - 新增 `hasData()` 函数判断课程是否有有效数据（积分、排名、测验、AI实践、分组任务）
    - 有数据的课程优先显示，查询不到数据的课程自动后排
    - 在有数据的课程中，按任务完成进度排序（完成度少的在前）
    - 排序逻辑应用于加载数据和查看全部页面两种场景

- **悬浮窗优化与 Bug 修复**:
    - 修复查看全部页面悬浮窗不显示的问题
    - 添加 `.detail-progress-item:hover .progress-tooltip { display: block }` 选择器
    - 添加 `.detail-progress-item .progress-tooltip` 响应式样式（居中显示在下方）
    - 清理悬浮窗中的调试 `console.log` 语句
    - 清理文件末尾冗余空行

- **代码清理**:
    - 移除所有悬浮窗调试代码
    - 移除冗余空行
    - 更新 README.md 更新日志，记录本次所有更新内容

- **版本号**:
    - 版本号保持 **2.2.0**

## 2026-01-10 进展

- **代码重构**:
    - 全面清理了 `chaoxing-list.user.js` 中的冗余注释和 AI 生成风格的解释，提升了代码整洁度。
    - 归档了之前实验性的 iframe 数据提取功能，保留了基础的 DOM 遍历与文本标签查找逻辑（`extractDataFromDomByLabel`）以备后用。
- **文档更新**:
    - 更新 `README.md` 至 v2.0.0，包含 UI 重构、仪表盘、待办任务聚合等主要功能特性。
- **版本发布**:
    - 版本号更新为 **2.0.0**，标志着 UI 重构版本的正式发布。
    - 完成代码提交与推送。

## 2026-03-12 进展（v2.1.0）

- **布局修复**:
    - 课程进度卡片（`.progress-card`）在宽屏下左右两列等宽问题已修复。
    - 给 `.progress-item` 添加 `min-width: 0` 和 `overflow: hidden`，防止内容撑破 grid 列，消除横向滚动条。
    - 移动端（`max-width: 768px`）下，进度卡片从跨双列改为单列堆叠（`grid-column: span 1`）。

- **忽略功能（新增）**:
    - 所有板块（待办任务、作业、考试、课程任务、课程进度）的每条课程信息均添加了 🚫 忽略按钮（hover 时显示）。
    - 点击忽略按钮弹出二次确认对话框，列出将被忽略的项目名称，支持点击遮罩取消。
    - 支持多选忽略：在"查看全部"详情页顶部点击"多选"切换多选模式，每项显示复选框，底部出现"忽略所选(N)"批量操作栏。
    - 忽略状态通过 `localStorage` 持久化存储（键名 `chaoxing_ignored_items`），刷新后保持。
    - 各板块使用 Vue `computed` 过滤已忽略项，忽略后立即从列表中消失。
    - 在"查看全部"详情页顶部新增"🚫 已忽略(N)"按钮，点击进入已忽略内容面板：
        - 显示所有已忽略项（标题、课程、忽略日期）。
        - 每项有"撤销忽略"按钮，支持多选后批量恢复。

- **版本号**:
    - 版本号更新为 **2.1.0**。
    - 完成代码提交与推送。

## 2026-03-13 进展（v2.2.0）

- **增强版课程进度悬浮窗（新增）**:
    - 在课程进度卡片中，当鼠标移动到某个课程时，显示增强版悬浮窗。
    - 悬浮窗展示该课程的所有学习活动，包括：
        - 📝 课程作业（显示标题和状态：进行中/已完成/未开始）
        - 📋 在线考试（显示标题和状态）
        - 📖 章节测验（显示标题和状态）
        - 💬 互动测验（包括讨论、抢答、评分等，显示标题和状态）
    - **智能数据加载**：
        - 鼠标悬停时自动通过 API 加载活动数据
        - 数据缓存机制，避免重复请求
        - 每个分类最多显示3条活动
        - 过滤掉签到、投票、直播等非学习类活动
    - **悬浮窗交互优化**：
        - z-index 提升至 10000，确保不被其他元素遮挡
        - 悬浮窗宽度从 240px 增加到 280px，提供更多空间
        - 添加最大高度和滚动条，防止内容过多溢出
        - 移动端自适应：小屏幕下悬浮窗居中显示在课程卡片下方
    - **视觉优化**：
        - 活动列表使用彩色边框区分状态：蓝色（进行中）、绿色（已完成）、灰色（未开始）
        - 每个活动显示状态标签（进行中/已完成/未开始）
        - 分类标题使用图标标识，易于识别
        - 加载状态友好提示："正在加载活动..."

- **技术实现细节**:
    - 新增 Vue 响应式数据：`courseActivitiesCache` 用于缓存活动数据，`hoveredCourseKey` 追踪当前悬停的课程
    - 新增函数：`fetchCourseActivitiesOnHover()` 用于悬停时获取数据，`handleProgressItemHover()` 和 `handleProgressItemLeave()` 处理悬停事件
    - 复用已有的 `fetchCourseActivities()` 函数获取课程活动列表
    - 增强 CSS 样式：新增 `.tooltip-activities-section`、`.activity-group-title`、`.activity-item` 等样式类
    - 响应式设计：添加 `!important` 规则确保移动端布局正确

## 2026-01-10 进展

- **代码重构**:
    - 全面清理了 `chaoxing-list.user.js` 中的冗余注释和 AI 生成风格的解释，提升了代码整洁度。
    - 归档了之前实验性的 iframe 数据提取功能，保留了基础的 DOM 遍历与文本标签查找逻辑（`extractDataFromDomByLabel`）以备后用。
- **文档更新**:
    - 更新 `README.md` 至 v2.0.0，包含 UI 重构、仪表盘、待办任务聚合等主要功能特性。
- **版本发布**:
    - 版本号更新为 **2.0.0**，标志着 UI 重构版本的正式发布。
    - 完成代码提交与推送。

## 2026-03-12 进展（v2.1.0）

- **布局修复**:
    - 课程进度卡片（`.progress-card`）在宽屏下左右两列等宽问题已修复。
    - 给 `.progress-item` 添加 `min-width: 0` 和 `overflow: hidden`，防止内容撑破 grid 列，消除横向滚动条。
    - 移动端（`max-width: 768px`）下，进度卡片从跨双列改为单列堆叠（`grid-column: span 1`）。

- **忽略功能（新增）**:
    - 所有板块（待办任务、作业、考试、课程任务、课程进度）的每条课程信息均添加了 🚫 忽略按钮（hover 时显示）。
    - 点击忽略按钮弹出二次确认对话框，列出将被忽略的项目名称，支持点击遮罩取消。
    - 支持多选忽略：在"查看全部"详情页顶部点击"多选"切换多选模式，每项显示复选框，底部出现"忽略所选(N)"批量操作栏。
    - 忽略状态通过 `localStorage` 持久化存储（键名 `chaoxing_ignored_items`），刷新后保持。
    - 各板块使用 Vue `computed` 过滤已忽略项，忽略后立即从列表中消失。
    - 在"查看全部"详情页顶部新增"🚫 已忽略(N)"按钮，点击进入已忽略内容面板：
        - 显示所有已忽略项（标题、课程、忽略日期）。
        - 每项有"撤销忽略"按钮，支持多选后批量恢复。

- **版本号**:
    - 版本号更新为 **2.1.0**。
    - 完成代码提交与推送。
## 2026-09-01 项目纳管阶段

- **已完成内容**：
  - 从 `https://github.com/Cooanyh/chaoxing-list` 克隆至“脚本猫”大项目下的独立目录 `chaoxing-list`。
  - 新增项目根目录 `Programs.md`，明确用途、来源与许可、独立管理边界，以及运行和真实页面验证提示。
  - 保留既有业务代码、历史进展和 Git 历史；本阶段未提交、未推送，也未改动脚本逻辑。

- **当前状态**：
  - 子项目目录已建立，`origin` 已指向指定 GitHub 仓库。
  - 当前任务仅完成基础纳管和文档完善，等待后续明确的功能或维护需求。

- **遇到的问题**：
  - 未发现子项目根目录 `AGENTS.md`；继续遵循大项目的统一协作与安全约束。

- **下一步计划**：
  - 后续需求先在本目录检查 Git 状态与相关代码，再进行定向修改。
- 涉及网页交互的改动，在已登录的真实学习通浏览器会话中进行验证。

## 2026-09-01：纳管文档版本管理

- 已检查本阶段变更，仅包括 `Programs.md` 与本进展记录，未涉及业务脚本。
- 下一步：以“docs: 初始化子项目说明与进展记录”提交并推送至该项目的既有 `origin`。

### 版本管理结果

- 已完成提交并推送：`16300b2 docs: 初始化子项目说明与进展记录`。
- 当前工作树干净；后续仅在收到明确需求时进行功能开发或验证。

## 2026-09-17：任务引擎与统一设置升级（进行中）

- **已完成内容**：确认子项目工作树干净、远程仍为 `Cooanyh/chaoxing-list`，并完成现有脚本与历史忽略实现的初步盘点。
- **当前状态**：待在用户登录后的真实学习通会话中采集旧版与新版任务引擎的页面结构、可用请求和任务分类映射；随后据此完成界面、统一设置及按课程忽略的设计与实现。
- **遇到的问题**：任务引擎链接需要登录态，不能以未认证页面结构替代真实验证。
- **下一步计划**：启动独立 Playwright 浏览器，由用户完成登录后进行只读页面勘察；在确认设计后分阶段开发、静态检查与真实页面验收。

### 2026-09-17 阶段一：真实任务引擎勘察完成

- **已完成内容**：在用户已登录的隔离浏览器会话中，只读核查了同一课程的旧版与新版任务页。新版页面通过跨域 iframe 呈现任务清单，真实可见“进行中/已结束”分段，以及分组任务、AI 实践、随堂练习、问卷、抢答、签到和通知等任务类型；旧版与新版均保留独立的“任务、作业、考试”等课程入口。
- **当前状态**：已确认新版课程学习数据存在任务点、作业、测验、考试、AI 实践等独立请求入口；下一阶段需要在脚本侧将其统一归一为任务类别，并设计统一设置与按课程过滤的数据模型。
- **遇到的问题**：任务引擎的 iframe 为跨域页面，不能将宿主 DOM 选择器当成稳定的数据源；部分旧的“unfinished”接口返回 404，因此实现必须以可用的新端点和现有回退路径为准。
- **下一步计划**：确认默认展示策略后，实施统一设置入口、忽略总开关、课程级多选忽略和任务引擎聚合；随后做静态检查与真实已登录页面验收。

### 2026-09-17 阶段二：本地实现完成，待安装态验收

- **已完成内容**：脚本版本更新至 `2.3.0`。新增统一设置页：默认仅显示核心学习任务，其他任务可单独开启；首次进入仪表盘会提示该入口。新增忽略总开关，未开启时不显示忽略、多选及已忽略入口。新增按课程多选忽略，使用课程 ID 与班级 ID 持久化，命中后跨待办、作业、考试、课程任务和课程进度自动过滤。
- **当前状态**：已将活动类型归一为作业、视频/任务点、AI 实践、分组讨论、测验、考试及其他任务；新版任务引擎 `job` 统计用于视频/任务点课程级摘要。待执行脚本语法、差异检查和已登录页面安装态验收。
- **遇到的问题**：新版 `job` 接口提供课程级完成统计而不是每一个视频任务点明细，故界面明确标注为“视频/任务点（完成数/总数）”，不伪造任务名。
- **下一步计划**：完成静态检查并在隔离浏览器进行已登录页面验收；不提交、不推送，等待用户确认后再进入版本管理。

### 2026-09-17 阶段三：静态验证完成

- **已完成内容**：`node --check chaoxing-list.user.js` 与 `git diff --check` 均通过。另以脚本内容核对确认：版本为 `2.3.0`、其他任务默认关闭、首次设置提示、忽略总开关默认关闭、课程级忽略键、跨板块过滤、新版 `study-data/v3/job` 适配以及视频/任务点、AI 实践、分组讨论分类均已存在。
- **当前状态**：本地变更已可供安装验证；浏览器已确认用户登录课程且新版任务引擎相关请求返回正常。
- **遇到的问题**：隔离 Playwright 浏览器未安装脚本管理器，不能将本地 Userscript 自动加载进真实课程页；因此尚未完成“已安装脚本的可视化验收”。
- **下一步计划**：由用户在已使用的脚本管理器中临时导入本地脚本后，复核设置入口、首次提示、其他任务开关和课程级忽略；保持本地修改，等待用户确认后再考虑提交和推送。

### 2026-09-17 阶段四：交互与课程级过滤修订完成

- **已完成内容**：按课程忽略改为“选择要忽略的课程”二级弹窗，设置页不再直接铺开课程清单。首次使用提示改为独立的浅蓝引导横条，包含图标、清晰文案和“去设置/知道了”操作；文案明确签到、问卷、抢答、通知等仅在设置中开启。
- **当前状态**：紧急待办区域已改为读取课程级过滤后的 `filteredUrgentTasks`；忽略一门课程后，待办、全部作业、全部考试、课程任务、课程进度及首页高优先级横条都会同步隐藏相关内容。
- **遇到的问题**：课程忽略状态的复选框需要依赖响应式版本号刷新，已在设置视图建立依赖，避免点击后视觉状态不同步。
- **下一步计划**：等待用户在脚本管理器的真实安装态检查上述交互；继续保持本地变更，不提交、不推送。

### 2026-09-17 阶段五：其他任务分类选择与卡片对齐完成

- **已完成内容**：将“显示其他任务”替换为“配置其他任务”弹窗。弹窗可选择“显示全部其他任务”，也可单独多选签到、问卷、抢答、通知、投票、直播、评分、拍照、笔记和未识别的其他任务；选择即时写入本地设置并同步过滤首页与各列表。仪表盘同一行卡片改为拉平高度，空板块会保留空间并展示“这里暂时还没有任务”说明。
- **当前状态**：核心任务默认展示，其他任务可按全部或指定类别展示；课程级忽略仍优先于所有展示分类规则和紧急待办。
- **遇到的问题**：无。
- **下一步计划**：等待真实安装态验收；继续保持本地变更，不提交、不推送。

### 2026-09-17 阶段六：去重与设置即时反馈完成

- **已完成内容**：移除任务清单中的视频/任务点课程级摘要，视频/任务点仅由课程进度板块呈现。所有设置变更（其他任务全选或多选、忽略总开关、课程忽略）均会立即写入本地并弹出约 2.6 秒的底部成功提示；现有计算属性会同步刷新首页、列表和高优先级待办。
- **当前状态**：任务分类不再重复展示视频/任务点，设置修改具备即时可见反馈。
- **遇到的问题**：无。
- **下一步计划**：等待真实安装态验收；继续保持本地变更，不提交、不推送。

### 2026-09-17 阶段七：设置通知时机修订完成

- **已完成内容**：弹窗内的勾选继续实时写入并应用，但不再逐项弹出提示；“配置其他任务”和“按课程忽略”仅在用户点击“完成”后显示保存成功通知。
- **当前状态**：设置应用与用户确认两个时机已分离，避免多选时连续弹出通知。
- **遇到的问题**：无。
- **下一步计划**：等待真实安装态验收；继续保持本地变更，不提交、不推送。

### 2026-09-17 阶段八：忽略课程跳过进度 API 完成

- **已完成内容**：课程进度加载在获取课程列表后，先按课程级忽略状态筛选；已忽略课程不会进入 `getCourseProgress`，因此不会发出课程进度相关请求。加载过程中如新忽略课程，尚未开始的该课程请求也会被再次检查并跳过。
- **当前状态**：已忽略课程同时在 UI 和课程进度请求层被排除；已发出的在途请求不会强制中断，但其结果仍会被 UI 过滤。
- **遇到的问题**：无。
- **下一步计划**：等待真实安装态验收；继续保持本地变更，不提交、不推送。

### 2026-09-17 阶段九：AI 实践归类与批量忽略完成

- **已完成内容**：AI 实践从课程任务列表移入全部作业列表，保留课程页跳转能力。设置页新增“批量忽略已完成或已过期内容”弹窗，可选择已完成作业（含 AI 实践）、已完成/过期考试、已结束课程任务及已完成课程进度，并显示当前可忽略数量。
- **当前状态**：批量操作按原板块写入既有忽略存储，过滤立即生效且可在已忽略面板恢复；课程级忽略不会被批量操作改变。
- **遇到的问题**：批量操作只处理当前已加载的数据，避免为了清理历史记录额外发起课程或任务请求。
- **下一步计划**：等待真实安装态验收；继续保持本地变更，不提交、不推送。

### 2026-09-17 阶段十：动态忽略与加载流程完成

- **已完成内容**：将“批量忽略”调整为可反复配置的动态规则，覆盖已完成作业（含 AI 实践）、已完成或过期考试、已结束课程任务和已完成课程进度。规则对以后加载或未来出现的匹配内容持续生效；取消勾选只停用相应规则，不会改写用户已有的逐条手动忽略记录。首页完整加载待办、作业、考试、课程任务、课程进度与高优先级信息；课程任务请求结果同时补入待办，避免为首页重复遍历全部课程。
- **当前状态**：动态规则、课程级忽略和逐条手动忽略相互独立；首页所有主卡片均为首屏加载，设置课程列表、进度悬浮详情等非首屏资源仍按需读取。
- **遇到的问题**：首屏完整性优先于过度延迟请求；通过复用课程任务读取结果，而非推迟主卡片加载，控制额外请求。
- **下一步计划**：执行语法与差异检查，并等待用户在脚本管理器中的真实安装态验收；按用户要求不提交、不推送。

### 2026-09-17 阶段十一：首页返回骨架屏过渡完成

- **已完成内容**：从统一设置返回首页时，先展示与现有仪表盘卡片布局一致的轻量骨架屏，再在约 180ms 后渲染真实首页。骨架屏覆盖欢迎区、紧急提醒条和任务卡片，避免设置页卸载、首页大列表重排与数据筛选同时发生时造成明显卡顿。
- **当前状态**：首页核心数据仍保持首屏加载；该过渡只改变返回时的视觉渲染顺序，不清空数据、不重新请求 API。
- **遇到的问题**：无。
- **下一步计划**：执行静态检查，等待真实安装态观察返回首页的过渡效果；不提交、不推送。

### 2026-09-17 阶段十二：骨架屏回退与加载恢复

- **已完成内容**：收到 ScriptCat 注入时的语法错误反馈后，已撤回阶段十一新增的骨架屏渲染分支与相关样式，恢复到此前可用的首页返回逻辑。
- **当前状态**：动态忽略、首页完整加载和课程级过滤均保留；当前脚本不含新增骨架屏代码。
- **遇到的问题**：本地 `node --check` 与函数级解析均未复现错误，浏览器日志中的错误来源显示为扩展注入地址且未提供 Userscript 源码位置，无法仅凭该日志精确归因。
- **下一步计划**：用户更新本地脚本后确认能加载；确认后使用更小、可逐步验证的方式重新实现返回过渡。

### 2026-09-17 阶段十三：设置返回首页骨架屏重新实现

- **已完成内容**：以更小的两阶段视图切换重新加入骨架屏：点击“返回仪表盘”后先进入独立的 `dashboard-loading` 静态骨架页，待浏览器完成一帧绘制并保持约 160ms 后，再切换到真实首页。骨架页只含固定欢迎区、提示条和三张卡片占位，不使用动态列表循环或动态类名。
- **当前状态**：首页数据不清空、不重新请求，首屏任务、课程和高优先级加载策略保持不变；返回时大列表重排与设置页卸载被拆分到两个渲染阶段。
- **遇到的问题**：上一版扩展注入报错无法由本地解析复现，故本版压缩了渲染分支和节点数量，并增加双重语法检查。
- **下一步计划**：在 ScriptCat 保存更新后确认真实页面加载与返回动画；不提交、不推送。

### 2026-09-17 阶段十四：骨架刷新动效与渐隐过渡完成

- **已完成内容**：骨架页新增“正在整理学习清单”刷新转圈。真实首页开始渲染后，骨架页会作为覆盖层保留在上方，并以 340ms 缓出渐隐消失，使骨架到真实卡片的变化连续、统一。
- **当前状态**：返回过程为“静态骨架与刷新提示 → 真实首页在覆盖层下准备 → 骨架缓慢淡出”；不影响首页数据、不增加接口请求。
- **遇到的问题**：无。
- **下一步计划**：执行静态检查并在 ScriptCat 的真实页面观察动效；不提交、不推送。

### 2026-09-17 阶段十五：课程进度悬浮卡自适应方向完成

- **已完成内容**：课程进度卡片悬停时会读取当前卡片相对视口的位置。若下方剩余空间不足以显示详情，悬浮详情会改为向上展开；空间足够时保持原有方向。详情页和首页课程进度卡片共用该判断，离开卡片后恢复默认状态。
- **当前状态**：底部课程的详情不再固定向下延伸到视口外。
- **遇到的问题**：无。
- **下一步计划**：执行静态检查并在真实页面验证底部与中部课程的不同展开方向；不提交、不推送。

### 2026-09-17 阶段十六：详情页状态信息去重完成

- **已完成内容**：待办、作业、考试和课程任务的“查看全部”详情列表移除了状态标签上方重复的浅灰状态/时间文本，仅保留现有彩色状态标签、“查看”和可选忽略操作。
- **当前状态**：首页概览仍保留时间与状态的紧凑双行信息；详情页采用更精简的一行状态设计。
- **遇到的问题**：无。
- **下一步计划**：执行静态检查并等待真实页面确认；不提交、不推送。

### 2026-09-17 阶段十七：向上课程悬浮卡越界展示完成

- **已完成内容**：当课程详情判定为向上展开时，脚本会临时解除课程进度卡片、内容区、网格及详情容器的 `overflow` 裁剪，让悬浮卡完整越出原有边界；鼠标离开后立即恢复原有滚动与裁剪行为。
- **当前状态**：底部课程的向上详情不会再被课程卡片边缘截断。
- **遇到的问题**：无。
- **下一步计划**：执行静态检查并在真实页面确认底部悬浮卡完整可见；不提交、不推送。

### 2026-09-17 阶段十八：详情入口、忽略开关与查询状态优化完成

- **已完成内容**：待办任务、课程任务和课程进度详情隐藏无对应原始页的“在原始页面打开”按钮，仅作业、考试保留该入口；统一设置中的忽略总开关改为带开关轨道、状态文案和键盘焦点样式的滑动控件；课程进度查询按钮会按真实状态显示“查询中”“已查询”“待查询”或“查询已关闭”。
- **当前状态**：课程进度首次读取完成后不会继续错误显示“查询中”，手动刷新期间才显示加载状态。
- **遇到的问题**：无。
- **下一步计划**：执行静态检查并等待真实页面确认；不提交、不推送。

### 2026-09-17 阶段十九：可选短时缓存与分区骨架屏完成

- **已完成内容**：统一设置新增默认关闭的“短时缓存”开关。开启后，课程任务和课程进度会读取 5 分钟内有效的本地缓存并立即展示，前台明确标注“缓存内容 · 正在更新”；后台请求成功后分别替换为最新数据并更新缓存。缓存会在启动、读写时检查过期，并按有效期定时自动清理。
- **当前状态**：待办、作业、考试、课程任务和课程进度的读取态统一改为带说明文字的骨架屏；各板块独立请求、独立结束加载，不需要等待全部请求完成。命中课程任务或课程进度缓存时，读取期间保留可读缓存而非覆盖为加载态。
- **遇到的问题**：无。
- **下一步计划**：执行静态检查；在真实安装态开启缓存后刷新页面，确认缓存标识、后台替换及 5 分钟过期行为；不提交、不推送。

### 2026-09-17 阶段二十：课程进度固定滚动区修复

- **已完成内容**：修正课程进度悬浮详情不应撑开主体卡片的行为。课程进度恢复与其他板块一致的固定内容区域和内部上下滚动，悬浮详情方向判断不再改变课程网格、内容区或卡片本身的高度。
- **当前状态**：课程列表在固定区域内自然滚动，不会脱离卡片背景或把页面异常撑高。
- **遇到的问题**：无。
- **下一步计划**：执行静态检查并在真实页面确认课程进度滚动行为；不提交、不推送。

### 2026-09-17 阶段二十一：课程进度详情稳定悬浮与首页滚动修复

- **已完成内容**：课程进度“查看全部”页的详情改为与首页相同的固定侧边悬浮，不再根据上下位置切换方向，避免页面反复重排抽搐。首页课程进度内容区明确固定为 350px 高度并启用内部纵向滚动、稳定滚动条和滚动边界控制，完整课程网格可在卡片内滚动查看。
- **当前状态**：详情页悬浮方向稳定；首页课程进度卡片不再截断未展示课程。
- **遇到的问题**：无。
- **下一步计划**：执行静态检查并在真实页面确认详情页无抖动、首页可滚动查看全部课程；不提交、不推送。

### 2026-09-17 阶段二十二：首页向上课程详情页面级浮层修复

- **已完成内容**：首页底部课程需要向上展示详情时，不再尝试突破内部滚动容器。脚本会在后台活动数据读取完成后克隆详情到页面顶层固定浮层，按课程卡片与视口的位置计算左右、上下坐标；离开课程后立即移除浮层。
- **当前状态**：课程进度主体继续保持固定 350px 内部滚动；向上详情不受卡片或滚动容器裁剪，可完整展示，两者互不影响。
- **遇到的问题**：无。
- **下一步计划**：执行静态检查并在首页底部课程验证完整浮层；不提交、不推送。

### 2026-09-17 阶段二十三：动态忽略内容实时预览完成

- **已完成内容**：动态忽略规则弹窗改为双栏布局。左侧选择规则，右侧实时预览当前会被匹配和隐藏的具体内容，显示类型、名称、所属课程及总数；预览列表独立滚动，数量超过 100 项时保留前 100 项并提示其余数量。
- **当前状态**：用户保存规则前即可核对影响范围；预览仅反映当前已加载数据，未来匹配项仍由动态规则自动处理。
- **遇到的问题**：无。
- **下一步计划**：执行静态检查并在真实设置页勾选不同规则确认预览同步变化；不提交、不推送。

### 2026-09-17 阶段二十四：动态忽略严格状态匹配修复

- **已完成内容**：动态忽略与预览改为共用严格状态判定。AI 实践仅在 `finished === true` 或状态明确为“已完成”时进入；普通作业、考试、课程任务、课程进度分别只接受明确的完成、过期、结束或完成率达到 100% 的条件。
- **当前状态**：进行中、未开始、状态缺失或未知的内容不会被动态规则选中、预览或隐藏。
- **遇到的问题**：无。
- **下一步计划**：执行静态检查并在真实设置页核对 AI 实践与进行中内容的预览边界；不提交、不推送。

### 2026-09-17 阶段二十五：v2.4.0 整理、归档与发布

- **已完成内容**：归档发布前完整的 v2.3.0 脚本快照；清理仪表盘中已过期或重复的版本/样式注释；将脚本元数据、README 徽标和更新日志升级至 v2.4.0。
- **当前状态**：已通过脚本语法与 Git 差异空白检查；已提交为 `4102e4d` 并推送至 `origin/main`。
- **遇到的问题**：无。
- **下一步计划**：在已安装脚本的真实课程页面进行交互验收；保留本地 `.playwright-cli/` 临时目录不入库。
